<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
         .chessboard {
            display: grid;
            grid-template-columns: repeat(8, minmax(0, 1fr));
            grid-template-rows: repeat(8, minmax(0, 1fr));
            width: 560px;
            height: 560px;
            border: 8px solid #815f3f;
            border-radius: 4px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        .light {
            background-color: #fff7e6;
            transition: background-color 0.2s;
        }
        .dark {
            background-color: #8b5e3c;
            transition: background-color 0.2s;
        }
        .piece {
            font-family: 'Noto Sans', sans-serif;
            font-size: 48px;
            line-height: 70px;
            cursor: pointer;
            user-select: none;
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.15s ease-in-out;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
        .piece:hover {
            transform: scale(1.1);
        }
        .piece[draggable="true"]:active {
            cursor: grabbing;
            transform: scale(1.15);
        }
        .piece.white-piece {
            color: #ffffff;
            text-shadow: 
                0px 0px 2px #000,
                1px 1px 2px rgba(0, 0, 0, 0.4);
        }
        .piece.black-piece {
            color: #1a1a1a;
            text-shadow: 
                0px 0px 2px #fff,
                1px 1px 2px rgba(255, 255, 255, 0.4);
        }
    </style>
</head>
<body class="bg-zinc-900 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-white text-3xl font-bold text-center mb-8">Chess Game</h1>
        
        <!-- Game Info -->
        <div class="text-white text-center mb-6">
            <div class="flex justify-center space-x-8">
                <div>White: <span id="whitePlayer">Waiting...</span></div>
                <div>Black: <span id="blackPlayer">Waiting...</span></div>
            </div>
            <div id="gameStatus" class="mt-2 text-emerald-400"></div>
        </div>

        <!-- Chess Board Container -->
        <div class="flex justify-center items-center">
            <div class="chessboard" id="chessBoard">
                <!-- Squares will be added dynamically -->
            </div>
        </div>

        <!-- Game Controls -->
        <div class="mt-6 flex justify-center space-x-4">
            <button id="resignBtn" class="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700 transition-colors">
                Resign
            </button>
            <button id="drawBtn" class="px-4 py-2 bg-yellow-600 text-white rounded hover:bg-yellow-700 transition-colors">
                Offer Draw
            </button>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.8.1/socket.io.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
    <script src="/js/chessGame.js"></script>
</body>
</html>